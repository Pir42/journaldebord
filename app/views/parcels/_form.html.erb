<%= form_with(model: parcel, local: true) do |form| %>
  <% if parcel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parcel.errors.count, "error") %> prohibited this parcel from being saved:</h2>

      <ul>
      <% parcel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group">
  <div class="field">
    <%= form.label :idPlace, value: 'ID du lieu' %>
    <%= form.text_field :idPlace, class: 'form-control' %>
  </div>
</div>

<div class="form-group">
  <div class="field">
    <%= form.label :name, value: 'Nom' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>
</div>

 <div class="actions mb-3">
    <% if current_page? new_parcel_path %>
      <%= form.submit 'Ajouter', class: 'btn btn-primary' %>
    <% else %>
      <%= form.submit 'Modifier', class: 'btn btn-primary' %>
    <% end %>
  </div>
<% end %>
