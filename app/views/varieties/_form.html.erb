<%= form_with(model: variety, local: true) do |form| %>
  <% if variety.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(variety.errors.count, "error") %> prohibited this variety from being saved:</h2>

      <ul>
      <% variety.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :vegetable_id %>
    <%= form.collection_select(:vegetable_id, Vegetable.where(user: current_user), :id, :name)%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
