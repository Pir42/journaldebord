<p id="notice"><%= notice %></p>

<h1>Action Types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @action_types.each do |action_type| %>
      <tr>
        <td><%= action_type.name %></td>
        <td><%= link_to 'Show', action_type %></td>
        <td><%= link_to 'Edit', edit_action_type_path(action_type) %></td>
        <td><%= link_to 'Destroy', action_type, method: :delete, data: { confirm: 'Etes-vous sûr ?' }, class: 'text-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Action Type', new_action_type_path %>
