<p id="notice"><%= notice %></p>

<div class="index">

  <h1>Types d'engrais</h1>

  <table class="table table-stripped table-hover table-responsive">
    <thead>
      <tr>
        <th scope="col">Nom</th>
        <th scope="col">Azote</th>
        <th scope="col">Phosphore</th>
        <th scope="col">Potassium</th>
        <th scope="col">Fournisseurs</th>
        <th scope="col">Détails</th>
        <th scope="col" colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @fertilizers.each do |fertilizer| %>
        <tr>
          <td><%= fertilizer.name %></td>
          <td><%= fertilizer.nitrogen %></td>
          <td><%= fertilizer.phosphorus %></td>
          <td><%= fertilizer.potassium %></td>
          <td><%= fertilizer.supplier %></td>
          <td><%= fertilizer.details %></td>
          <td><%= link_to 'Voir', fertilizer %></td>
          <td><%= link_to 'Modifier', edit_fertilizer_path(fertilizer) %></td>
          <td><%= link_to 'Supprimer', fertilizer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

<%= link_to 'Ajouter un engrais', new_fertilizer_path, class: 'btn btn-primary' %>

</div>
